# customer-service-request-system
C++ console-based Customer Service Request System. Queue (FIFO) manages active requests; Stack (LIFO) handles cancelled request recovery. Features request creation, status updates, search by email, resolved/cancelled filtering, and persistent menu loop. Built with singly linked lists and dynamic memory management.
